---
const team = [
  {
    name: 'Steve Spencer',
    role: 'Co-Owner & Project Manager',
    email: 'Steve@propertyprosmn.com',
    image: 'https://images.unsplash.com/photo-1560250097-0b93528c311a?w=400&q=80', // <!-- REPLACE: Steve Spencer photo -->
  },
  {
    name: 'Kevin Spencer',
    role: 'Co-Owner & Operator',
    email: 'Kevin@propertyprosmn.com',
    image: 'https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=400&q=80', // <!-- REPLACE: Kevin Spencer photo -->
  },
];
---

<section class="section-dark">
  <div class="grid lg:grid-cols-2">
    <!-- Left - Stats & Text -->
    <div class="padding-global py-20 lg:py-26 flex flex-col justify-center">
      <div class="container-large">
        <!-- Label -->
        <span class="section-label">Our Team</span>

        <!-- Title -->
        <h2 class="section-title text-white mb-4">
          Meet the Owners
        </h2>

        <!-- Lead Paragraph -->
        <p class="text-lg text-white/90 mb-4 max-w-md leading-relaxed">
          Brothers Steve and Kevin Spencer founded Property Pros with a simple mission: deliver quality drywall work and treat every customer like a neighbor.
        </p>

        <!-- Supporting Paragraph -->
        <p class="text-muted mb-8 max-w-md">
          As a family-owned business based in Clear Lake, we take personal pride in every project. When you work with Property Pros, you work directly with the owners.
        </p>

        <!-- Stats Row -->
        <div class="team-stats-row">
          <div class="team-stat-item">
            <div class="team-stat-value">15+</div>
            <div class="team-stat-label">Years Experience</div>
          </div>
          <div class="team-stat-divider"></div>
          <div class="team-stat-item">
            <div class="team-stat-value">500+</div>
            <div class="team-stat-label">Projects</div>
          </div>
          <div class="team-stat-divider"></div>
          <div class="team-stat-item">
            <div class="team-stat-value">Family</div>
            <div class="team-stat-label">Owned & Operated</div>
          </div>
        </div>

        <!-- CTA Button -->
        <div class="mt-8">
          <a href="/about" class="btn btn-outline-light">
            Get to Know Us
            <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
            </svg>
          </a>
        </div>
      </div>
    </div>

    <!-- Right - Team Grid -->
    <div class="padding-global py-12 lg:py-20 bg-brand-navy/50">
      <div class="team-grid-duo max-w-2xl mx-auto">
        {team.map((member) => (
          <div class="team-card-large">
            <img
              src={member.image}
              alt={member.name}
              class="team-image"
            />
            <div class="team-info">
              <div class="team-name">{member.name}</div>
              <div class="team-role">{member.role}</div>
              <a href={`mailto:${member.email}`} class="team-email">{member.email}</a>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
